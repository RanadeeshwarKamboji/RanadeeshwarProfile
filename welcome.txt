Hi, 
welcome to Ranadeeshwar profile,
Thanks For visiting our page.

git clone https://github.com/RanadeeshwarKamboji/RanadeeshwarProfile.git
git status 

git checkout -b featute/styles
git add styles.css 
git commit -m"Added styles.css file"
git push origin feature/styles 


Branch Commands:
git branch 
git checkout #branchName
git checkout -b #newBranchNameToBeCraeted
git branch -M styles
git branch -d styles

Merge Commands:

git diff #branchNameToCompare

if there are no Merge Conflicts, we can merge the code using following command

git merge #branchNameToMerge

Reset Commands:
git reset Head~1 
git reset #hashOfCommit
